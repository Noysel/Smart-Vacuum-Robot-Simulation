package bgu.spl.mics.application.objects;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import java.util.List;

class FusionSlamTest {
/* 
    @Test
    void testTransformTrackedObjectsToLandmarks() {
        FusionSlam fusionSlam = new FusionSlam();
        
        // Prepare mock tracked objects
        List<TrackedObject> trackedObjects = List.of(
            new TrackedObject(1, "Object1", 10.0, 20.0),
            new TrackedObject(2, "Object2", 30.0, 40.0)
        );

        // Transform tracked objects to landmarks
        List<Landmark> landmarks = fusionSlam.transformTrackedObjectsToLandmarks(trackedObjects);

        // Assertions
        assertNotNull(landmarks);
        assertEquals(2, landmarks.size());
        assertEquals("Object1", landmarks.get(0).getDescription());
        assertEquals("Object2", landmarks.get(1).getDescription());
    }
        */
}
